import 'package:flutter/material.dart';

class MinimizeOverlayScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
            'minimise overlay screen'
        ),
      ),
    );
  }
}