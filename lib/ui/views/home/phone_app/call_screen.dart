import 'package:flutter/material.dart';

class CallScreenPhoneApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Color.fromRGBO(8, 19, 23, 1),
      child: Center(
        child: Text(
            'contacts screen'
        ),
      ),
    );
  }
}