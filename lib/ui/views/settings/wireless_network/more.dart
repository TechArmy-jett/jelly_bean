import 'package:flutter/material.dart';

class WirelessNetworkMoreScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
            'wireless network more screen'
        ),
      ),
    );
  }
}