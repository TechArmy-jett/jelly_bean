import 'package:flutter/material.dart';

class DataTimeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
            'date time screen'
        ),
      ),
    );
  }
}