import 'package:flutter/material.dart';

class BackupResetScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
            'back up reset screen'
        ),
      ),
    );
  }
}