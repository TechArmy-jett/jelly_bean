import 'package:flutter/material.dart';

class LanguageInputScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
            'language input screen'
        ),
      ),
    );
  }
}